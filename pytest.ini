[pytest]
# Collect only the real test suite. submission1/ (and any future submission
# package) is a full copy of the repo, which otherwise causes duplicate test
# module basenames and import-file-mismatch collection errors.
testpaths = tests
norecursedirs = .venv venv submission* out data .git __pycache__ .pytest_cache
